{
  "name": "_sandbox",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "sync-vaults": "node scripts/sync-vaults.js",
    "sync-canvas": "node scripts/sync-canvas-to-md.js",
    "watch-vaults": "node --watch scripts/sync-vaults.js",
    "dev": "npm run sync-vaults && vite",
    "build": "npm run sync-vaults && vite build",
    "preview": "vite preview"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "dependencies": {
    "js-yaml": "^4.1.1",
    "marked": "^15.0.0"
  }
}
